# APEX CI — known-failing baseline (the "ratchet").
#
# CI deselects these so the gate is green today and blocks all NEW breakage.
# Delete lines as you fix them; when empty, drop the deselect logic and delete
# this file.
#
# Burndown: 17 -> 10 -> 3. The historical_readiness (4) and institutional_research
# (3) clusters are now FIXED AT THE SOURCE (institutional_data_quality._conn now
# resolves the module DB_PATH like its sibling modules) and are no longer parked.

# ── Needs individual triage (not yet root-caused) ─────────────────────────────
tests/test_institutional_roadmap.py::test_conviction_blocking_condition
tests/test_apex16_2_adaptive_trade_management.py::test_routes_present

# ── REAL config-drift finding (not a test bug) ────────────────────────────────
# ~31 env vars are read in code (os.getenv) but absent from the config REGISTRY,
# incl. safety-relevant ones (ANTHROPIC_API_KEY, ETRADE_OAUTH_TOKEN_*, the
# APEX_BRIEF_AI_* knobs, APEX_CALIBRATION_DB, APEX_EXECUTION_PREVIEW_TTL_SECONDS).
# Register them (with correct category/classification/safety_critical) to close it.
tests/test_pre23_hardening.py::test_registry_has_no_production_env_drift
