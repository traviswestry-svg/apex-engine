services:
  - type: cron
    name: apex-engine-scan
    env: python
    schedule: "*/30 13-21 * * MON-FRI"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python apex_engine_v1.py"
