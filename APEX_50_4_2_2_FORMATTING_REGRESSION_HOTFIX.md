# APEX 50.4.2.2 — Formatting Regression Hotfix

Fixes the Morning Brief crash caused by applying numeric `:f` formatting to categorical values such as `HIGH`, `neutral_gamma`, and `NOT_APPLICABLE`.

Also updates Morning Brief error responses to report the active build version instead of the stale application constant `48.2.0`.
